/**
 * sum
 * @param {number} m base
 * @param {number} n index
 * @returns {number}
 */
export default function sum(m, n) {
  const number1 = parseInt(m, 10);
  const number2 = parseInt(n, 10);
  return number1 + number2;
}
